[project]
name = "gg-shared"
version = "1.0.0"
description = "Shared constants and enums for gg.gram backend and bot"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["gg_shared*"]
